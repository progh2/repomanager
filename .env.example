# Optional: still works if set (highest priority)
GITHUB_TOKEN=

# OAuth App Client ID for "Sign in with GitHub" (Device Flow)
# Create at https://github.com/settings/developers
GITHUB_OAUTH_CLIENT_ID=
